<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:SQLSchemaCompare.AvaloniaUI.ViewModels"
             xmlns:v="using:SQLSchemaCompare.AvaloniaUI.Views"
             xmlns:local="using:SQLSchemaCompare.AvaloniaUI"
             x:Class="SQLSchemaCompare.AvaloniaUI.App"
             RequestedThemeVariant="Default">
             <!-- "Default" ThemeVariant follows system theme variant. "Dark" or "Light" are other available options. -->
    <Application.DataTemplates>
        <local:ViewLocator/>
    </Application.DataTemplates>
    <Application.Styles>
        <FluentTheme />
    </Application.Styles>
</Application>
